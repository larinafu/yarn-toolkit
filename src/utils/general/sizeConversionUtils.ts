export const pxToVh = (px: number) => (100 * px) / window.innerHeight;

export const vwToPx = (vw: number) => (window.innerWidth * vw) / 100;

export const vhToPx = (vh: number) => (window.innerHeight * vh) / 100;
